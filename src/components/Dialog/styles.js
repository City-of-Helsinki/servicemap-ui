export default theme => ({
  topCloseButton: {
    position: 'absolute',
    right: theme.spacing(2),
    top: theme.spacing(2),
  },
  closeButton: {
    marginRight: 0,
  },
  root: {
    padding: theme.spacing(3),
  },
});
