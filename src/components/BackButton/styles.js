
export default theme => ({
  containerButton: {
    zIndex: 0,
    color: 'inherit',
    padding: theme.spacing(1),
  },
  containerText: {
    color: 'inherit',
    fontSize: 14,
    paddingLeft: theme.spacing(1),
  },
});
